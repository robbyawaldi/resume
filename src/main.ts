import App from './App.svelte';
// import ForNadia from './ForNadia.svelte'

const app = new App({
	target: document.body,
});

// const app = new ForNadia({ target: document.body })

export default app;